{
  "name": "cypress",
  "version": "1.0.0",
  "description": "Cypress e2e testing showcase.",
  "scripts": {
    "test:cy": "npx cypress run --config-file config/cypress.config.ts",
    "test:cy:ui": "npx cypress open --config-file config/cypress.config.ts"
  },
  "author": "Lonnie Lund",
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "@testing-library/cypress": "^10.1.0",
    "cypress": "^15.10.0"
  }
}
